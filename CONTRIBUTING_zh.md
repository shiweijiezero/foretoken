# 为 Foretoken 做贡献

[English](CONTRIBUTING.md) | 简体中文

欢迎你参与 Foretoken，也感谢你愿意和我们一起把它做得更好。无论是代码、文档、测试、问题反馈还是设计讨论，我们都认真对待。我们希望每项合入的能力都有明确边界，能够解释、验证并长期维护。

所有参与者都应遵守 [Foretoken Community Code of Conduct](CODE_OF_CONDUCT.md)。

## 开发原则

- 先说明问题，再写解决方案。
- 一个 PR 只解决一类问题。不要把重构、功能、依赖升级和格式化混在一起。尽量控制PR的规模在一千行代码内。
- 要充分测试功能，不只证明代码能编译、进程能启动或对象能创建。
- 实验功能默认关闭，并提供明确的启用方式和失败边界。
- 提交者对全部代码负责与审阅，包括 AI 辅助生成的代码，并应能够解释设计和实现。
- 稳定接口不能静默破坏。需要不兼容变更时，应先提供版本、弃用周期和迁移方式。
- 每个文件开头需要注明开源协议，并简洁描述文件功能。
- PR 应当通过CI与格式检测。
- 所有 PR 应当被至少一名非作者维护者审阅。

## 开始开发前

以下修改可以直接提交小型 PR：

- 文档、拼写和链接修复；
- 有明确复现方式的局部缺陷修复；
- 不改变外部行为的局部整理；
- 为已有行为补充测试。

以下修改应先创建 Issue 或设计提案，确认目标、边界和维护责任后再实现：

- 新增或修改 CRD、CLI、配置格式、公开 Go/Python API；
- 新增控制器、路由器、扩缩容器、运行时后端或硬件后端；
- 修改控制面与数据面之间的协议；
- 引入新的外部依赖、测试体系或常驻 CI 任务；
- 跨组件重构、目录重组或部署方式变更；
- 可能影响兼容性、性能结果或资源开销的改动。

Bug Issue 应包含复现步骤、预期行为、实际行为、环境和最小日志。不要只提交截图或一句“不能工作”。

重大修改应先创建标题以 `[Proposal]` 开头的 Issue。设计说明至少应包含：

- 问题、背景和使用场景；
- 目标、非目标和影响范围；
- 方案、接口或数据流；
- 被否决的备选方案及原因；
- 兼容性、升级与回退方式；
- 测试、观测和成功标准；
- 依赖、CI 成本和后续维护者。

开始实现前应取得受影响模块维护者的认可。设计提案获批表示方向可以实施，不代表后续代码可以免于 Review。

## 代码边界

Foretoken 的主要目录承担不同职责：

- `data-plane/`：请求处理、路由、推理引擎接入和运行时数据路径；
- `control-plane/`：期望状态、实例生命周期、扩缩容决策、Kubernetes 资源渲染和故障恢复；
- `benchmarks/`：正确性、随机负载、SLO 和仿真评测；
- `deploy/`：部署组合、硬件配置和发布制品；

模块内部的单元测试应与源码放在一起。根目录 `tests/` 不用于堆放可以归属于单个模块的测试。

## 提交 Pull Request

PR 描述应包含：

- 要解决的问题和复现方式；
- 本次修改的范围与非目标；
- 关键设计选择；
- 执行过的验证命令及结果；
- 未验证内容、兼容性风险和必要的回退方式；
- 关联的 Issue 或设计提案。

面向用户的行为、命令、配置或状态发生变化时，应同步更新相关中英文文档、示例和 Release Note。PR 尚未准备好接受完整 Review 时，请使用 Draft 状态。

请保持 PR 可审阅：

- 不提交密钥、令牌、服务器地址、私有 kubeconfig、模型凭据或本地实验数据；
- 不提交与本次修改无关的格式化和生成文件；
- 不用空测试、被注释掉的测试或仅初始化对象的测试充当覆盖；
- 不因自动化工具给出建议就直接接受，提交者需要判断其正确性；

AI 辅助贡献必须遵守同样的标准。提交者不得把密钥、私有代码、服务器配置或未公开数据交给外部模型；必须核对代码来源和许可证；不得声称执行过实际没有运行的命令、测试或硬件验证。自动生成的说明不能代替对代码的理解。

实验性能力应默认关闭。合并前至少需要一名非作者维护者批准，并通过适用于本次变更的必需检查。

## 分支与提交信息

分支名称应简短说明目的，例如：

```text
feature/control-plane-baseline
fix/router-timeout
docs/contributing-guide
benchmark/slo-simulation
```

提交信息采用 Conventional Commits 风格：

```text
feat(control-plane): add inference group reconciliation
fix(router): handle unavailable backends
test(bench): cover SLO search boundaries
docs: add development guidelines
```

常用前缀包括 `feat`、`fix`、`docs`、`test`、`refactor`、`perf`、`ci` 和 `chore`。提交信息应描述实际变化，不写笼统的“update”或“fix issues”。

## 许可证

提交到 Foretoken 的内容将按照仓库的 [Apache License 2.0](LICENSE) 发布。请确保你有权提交相关代码、文档、数据和测试材料。
