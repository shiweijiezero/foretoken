apiVersion: orchestration.foretoken.ai/v1alpha1
kind: InferenceGroup
metadata:
  labels:
    app.kubernetes.io/name: control-plane
    app.kubernetes.io/managed-by: kustomize
  name: inferencegroup-sample
spec:
  # TODO(user): Add fields here
