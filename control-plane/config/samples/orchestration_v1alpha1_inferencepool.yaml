apiVersion: orchestration.foretoken.ai/v1alpha1
kind: InferencePool
metadata:
  labels:
    app.kubernetes.io/name: control-plane
    app.kubernetes.io/managed-by: kustomize
  name: inferencepool-sample
spec:
  # TODO(user): Add fields here
