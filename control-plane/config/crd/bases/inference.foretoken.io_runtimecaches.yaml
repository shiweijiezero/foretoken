# SPDX-License-Identifier: Apache-2.0
# SPDX-FileCopyrightText: Copyright contributors to the Foretoken project
#
# Defines a generated Kubernetes API manifest for Foretoken custom resources.
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: runtimecaches.inference.foretoken.io
spec:
  group: inference.foretoken.io
  names:
    kind: RuntimeCache
    listKind: RuntimeCacheList
    plural: runtimecaches
    singular: runtimecache
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.claimName
      name: Claim
      type: string
    - jsonPath: .status.capacity
      name: Capacity
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RuntimeCacheSpec defines one platform-managed persistent
              cache volume.
            properties:
              accessMode:
                default: ReadWriteMany
                description: RuntimeCacheAccessMode selects the Kubernetes PVC access
                  mode.
                enum:
                - ReadWriteOnce
                - ReadWriteMany
                type: string
              directory:
                description: |-
                  Directory is a deployment-root-relative directory for a static hostPath PV.
                  Declaring it means the resolved path is already shared at the same location on every target node.
                  The deploy command resolves the path but never uploads its contents or installs shared storage.
                maxLength: 4096
                minLength: 1
                type: string
              initialSize:
                description: |-
                  InitialSize is the first PVC request for dynamic storage. Directory mode uses
                  an internal bookkeeping capacity when this field is omitted.
                maxLength: 64
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: must be a valid non-negative Kubernetes resource quantity
                  rule: isQuantity(self) && quantity(self).compareTo(quantity('0'))
                    >= 0
              maxSize:
                description: MaxSize enables automatic expansion when set and may
                  only be increased.
                maxLength: 64
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: must be a valid non-negative Kubernetes resource quantity
                  rule: isQuantity(self) && quantity(self).compareTo(quantity('0'))
                    >= 0
              retentionPolicy:
                default: Retain
                description: RuntimeCacheRetentionPolicy controls PVC cleanup when
                  a RuntimeCache is deleted.
                enum:
                - Delete
                - Retain
                type: string
              storageClassName:
                type: string
            type: object
            x-kubernetes-validations:
            - message: initialSize must be positive
              rule: '!has(self.initialSize) || quantity(self.initialSize).compareTo(quantity(''0''))
                > 0'
            - message: maxSize must be positive
              rule: '!has(self.maxSize) || quantity(self.maxSize).compareTo(quantity(''0''))
                > 0'
            - message: maxSize must be greater than initialSize
              rule: '!has(self.maxSize) || !has(self.initialSize) || quantity(self.maxSize).compareTo(quantity(self.initialSize))
                > 0'
            - message: initialSize is required when directory is not set
              rule: has(self.directory) || has(self.initialSize)
            - message: only maxSize may change
              rule: has(self.storageClassName) == has(oldSelf.storageClassName) &&
                (!has(self.storageClassName) || self.storageClassName == oldSelf.storageClassName)
                && has(self.initialSize) == has(oldSelf.initialSize) && (!has(self.initialSize)
                || self.initialSize == oldSelf.initialSize) && self.accessMode ==
                oldSelf.accessMode && self.retentionPolicy == oldSelf.retentionPolicy
                && has(self.directory) == has(oldSelf.directory) && (!has(self.directory)
                || self.directory == oldSelf.directory)
            - message: maxSize cannot be removed or decreased
              rule: '!has(oldSelf.maxSize) || (has(self.maxSize) && quantity(self.maxSize).compareTo(quantity(oldSelf.maxSize))
                >= 0)'
            - message: directory cannot be combined with PVC size or storageClassName
              rule: '!has(self.directory) || (!has(self.maxSize) && !has(self.initialSize)
                && !has(self.storageClassName))'
          status:
            description: RuntimeCacheStatus publishes the managed claim and its observed
              capacity.
            properties:
              capacity:
                description: ResourceQuantity is a non-negative Kubernetes resource
                  quantity.
                maxLength: 64
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: must be a valid non-negative Kubernetes resource quantity
                  rule: isQuantity(self) && quantity(self).compareTo(quantity('0'))
                    >= 0
              claimName:
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                format: int64
                type: integer
              phase:
                description: RuntimeCachePhase summarizes the managed PVC lifecycle.
                enum:
                - Pending
                - Ready
                - Resizing
                - Degraded
                - Terminating
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
