## 动机
本 PR 解决的问题 / 关联 issue。

## 改动
要点列表(改了什么、为什么)。

## 验证
- [ ] `ruff check` 通过
- [ ] `pytest -m "not slow"` 通过
- [ ] 涉及 GPU / 端到端的改动已在 GPU 机器跑过(附 run 摘要或 `pytest -m slow` 结果)
- [ ] 新增 `.py` 已加 SPDX header

## 备注
对评测结果 / 兼容性 / 文档的影响。
