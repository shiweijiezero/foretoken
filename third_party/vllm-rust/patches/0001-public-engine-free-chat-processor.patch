diff --git a/rust/src/chat/src/lib.rs b/rust/src/chat/src/lib.rs
index 0d538a24ba1acb9aa856d914f2c51086c3a26b87..a33843d4a5b5e8c9488c53fe751649ef0e1498b0 100644
--- a/rust/src/chat/src/lib.rs
+++ b/rust/src/chat/src/lib.rs
@@ -99,9 +99,11 @@ pub struct ChatRequestProcessor {
 }
 
 impl ChatRequestProcessor {
-    /// Create a processor with multimodal support using the effective model
-    /// dtype reported by the engine.
-    fn new(backend: DynChatBackend, model_dtype: ModelDtype) -> Self {
+    /// Create an engine-free processor with multimodal support.
+    ///
+    /// Callers that prepare requests outside an EngineCore handshake provide the
+    /// model dtype used for multimodal tensor encoding.
+    pub fn with_model_dtype(backend: DynChatBackend, model_dtype: ModelDtype) -> Self {
         Self {
             backend,
             model_dtype: Some(model_dtype),
@@ -198,7 +200,7 @@ impl ChatLlm {
 
         Self {
             text,
-            processor: ChatRequestProcessor::new(backend, model_dtype),
+            processor: ChatRequestProcessor::with_model_dtype(backend, model_dtype),
             tool_call_parser: ParserSelection::Auto,
             reasoning_parser: ParserSelection::Auto,
         }
