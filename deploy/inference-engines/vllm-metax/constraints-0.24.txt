# Transformers 5.16 imports torchaudio for text models, exposing the MetaX 2.4.1 wheel ABI mismatch with Torch 2.10.
# Version 5.5.3 satisfies vLLM and vLLM-metax 0.24 requirements and keeps audio imports lazy.
transformers==5.5.3
# TVM FFI 0.1.9 supports both XGrammar 0.2.1 and MetaX TileLang 0.1.9+maca.
# The object registration API in 0.1.13 cannot load the TVM bundled with that TileLang release.
xgrammar==0.2.1
apache-tvm-ffi==0.1.9
